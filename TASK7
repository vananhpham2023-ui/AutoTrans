数据集扩展与模型重训练
网络结构改进与时序信息融合
输出滤波与估计稳定化
归一化参数管理与版本控制
验证指标与评估流程
下面是每一节可直接写入 TASK7 的要点和可操作步骤。

1. 数据增强与模型重训练

1.1 数据采集与覆盖范围

目标：让数据分布覆盖当前「高动态 + 大外力」场景，避免 PINN 处于 OOD。
操作步骤：
使用 pinn_data_recorder.py 在以下场景分别录制：
无风 / 中风 / 强风（对应 so3_quadrotor 的不同 wind 配置）；
小幅圆轨迹、大幅圆轨迹、figure-eight、circle；
不同负载质量（若支持参数切换）。
每个场景运行 5个 完整周期，保证稳态段数据充足。
统一采样配置：与控制器一致（400 Hz IMU, 20 Hz cutoff 等），确保训练–部署分布一致。
1.2 数据预处理与清洗

步骤：
合并多次运行的 CSV 为统一数据集；
丢弃含 NaN/Inf 或明显异常（|thrust_total|、|acc| 超物理上限）的样本；
可选：对不同场景做均衡采样（每种风场/轨迹采样数量相近），避免单一场景主导训练；
划分训练/验证/测试集（例如 70/15/15），按「运行粒度」划分，避免同一轨迹切分到不同集合。
1.3 训练超参数与策略

模型：保留现有 FNN 结构做基线（13→128×5→6），在下一节再改结构。
推荐超参数范围（需在 TASK7 中说明为「建议起点」）：
优化器：Adam 或 AdamW；
初始学习率 1e-3，采用 cosine decay 或 step decay（例如每 20k step 衰减 ×0.5）；
批大小：4096–16384（根据显存尝试）；
训练迭代：以「验证损失收敛 + 物理约束项稳定」为准，一般 100–300k step；
损失：L = w_data * MSE(force) + w_phys * L_phys + w_reg * ||θ||²，建议记录不同 w_phys 下的效果。
交叉验证：
按场景（不同风/轨迹）做 K-fold：每次留出 1–2 个场景作为验证，其余场景训练；
检查各折上的 RMSE 和物理残差（力平衡误差）是否一致，避免某些场景明显过拟合或欠拟合。
1.4 重训练输出与模型选择

为每次训练保存：
最佳验证损失模型（best_val.pt）；
若使用 early stopping，记录停止时 step；
各场景 RMSE/物理残差表格。
选择最终部署模型时，优先考虑「多场景平均性能最好」而非单场景最优。
2. 网络结构改进与时序信息融合

2.1 时间窗口机制

输入从「单帧 13 维」改为「时间窗口 T 帧 × 13 维」，例如：
T=5 或 10，对应 12.5–25 ms 历史；
将窗口展平为 13*T 维向量或按 (T, 13) 喂入 RNN/LSTM。
具体设计选项（TASK7 中可给方案 A/B）：
方案 A：FNN + 展平窗口：输入维度 = 13*T，继续使用多层全连接；
方案 B：LSTM/GRU + FNN：(T,13) → LSTM hidden state → 全连接输出 6 维力。
2.2 LSTM / 时序模块配置

建议起点：
LSTM 层数 1–2 层，hidden size 64–128；
仅使用最后一个 time step 的隐藏状态用于回归；
在输出层前加一层全连接 + tanh 以限制输出幅度。
训练注意：
数据加载时按滑动窗口构造训练样本，保证时间顺序；
可使用「teacher forcing」风格的训练脚本，但预测阶段仍是单步前向。
2.3 模型复杂度与推理效率平衡

目标：保证在 100–200 Hz 控制频率下，单次推理延迟 < 1–2 ms。
TASK7 中建议：
在 Python 端预先 benchmark 各候选结构（FNN、FNN+窗口、LSTM）在 CPU 上的推理时间；
排除延迟超标的结构，只保留满足时延约束的版本进入 C++ 集成。
3. 滤波算法优化与实现

3.1 输出滤波策略设计

组合方案建议：
短期平滑：指数滑动平均（EMA），alpha ~ 0.3–0.6；
中期平滑：二阶低通 LowPassFilter2p（已经在 C++ 实现），截止频率在 8–15 Hz 之间调参；
可选：对估计误差（估计–LBFGS 或 估计–真值）做简单一阶 Kalman 滤波/滑动平均，用于离线评估和在线监控。
3.2 选择滤波算法与参数

在 TASK7 中给出对比：
纯 EMA vs. 纯 LPF vs. EMA+LPF，在离线数据上的 RMSE 与相位延迟；
通过 Bode 曲线或简单阶跃响应展示延迟不超过若干 ms。
参数调优流程：
在记录好的 force CSV 上重放 PINN 原始输出，试不同 alpha、cutoff_freq；
按「RMSE + 延迟」联合指标选择最佳组合。
3.3 延迟评估

定义延迟指标：峰值位置滞后、90% 上升时间等；
确保总延迟（EMA+LPF）低于 MPC 控制周期的 1–2 个采样周期。
4. 归一化参数管理与一致性保障

4.1 归一化参数生成与固化

训练前：
使用 compute_normalizer_stats.py 在最终训练集上生成 normalizer_stats.json；
固定该文件为「当前模型版本的唯一合法归一化配置」。
训练脚本：
强制从 normalizer_stats.json 中读取 min/max（或 mean/std），禁止在训练时临时重算。
4.2 部署一致性与版本控制

在 C++ 端：
PinnForceEstimator 仅从参数指定的 normalizer_stats.json 读取归一化参数；
在启动时打印模型路径 + normalizer 路径 +摘要（如 samples，files），方便核对。
版本管理：
TASK7 中建议为「(model.pt, normalizer.json)」建立版本号，如 pinn_v1.2.0；
每次重训必须同步升级版本号，并在 YAML 中显式指定使用哪一版。
4.3 异常监控

在 debug_pinn_raw_outputs.py 或 C++ 日志中增加检查：
若原始网络输出长期超出 [-1,1] 很多（例如 |output|>5），提示归一化/数据分布不匹配；
记录这些样本供后续扩充训练集。
5. 验证与评估体系

5.1 指标设计

精度类：
RMSE(fl)、RMSE(fq)，分别按分量和模长计算；
物理一致性指标：力平衡、矩平衡残差的均值 + 标准差。
稳定性类：
输出方差 / 高频能量（例如 >20 Hz 频域能量）；
饱和统计：估计模长接近 max_force 的时间占比。
性能类：
单次推理时间分布（均值、P95、P99）；
CPU 利用率和内存占用。
5.2 测试场景

至少涵盖：
训练集内的典型轨迹（sanity check）；
更激烈轨迹/更大风场（泛化能力）；
启动瞬态 / 急转弯 / 强风突变 等边界工况。
5.3 对比与报告

对比对象：
旧版 PINN；
LBFGS 估计器（作为参考上界）；
无外力估计时的 MPC 性能（基线）。
报告内容：
各场景 RMSE、稳定性指标表格与对比条形图；
force 对比曲线（类似当前 force_manual_run_*.png），对齐时间段直接展示跟踪效果；
执行效率统计表；
关键结论与推荐的部署配置（模型版本、滤波参数等）。